# Frontend Test Dockerfile
# Purpose: Create a containerized frontend environment for testing
# This matches the CI/CD environment for consistency

FROM node:20-alpine

# Install curl for healthchecks
RUN apk add --no-cache curl

# Set working directory
WORKDIR /app

# Copy package files
COPY package*.json ./

# Install dependencies
RUN npm ci

# Copy application code
COPY . .

# Expose port
EXPOSE 5173

# Start dev server (for testing)
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]